export enum FormDirection {
  VERTICAL = "vertical",
  HORIZONTAL = "horizontal",
}

export enum Status {
  ACTIVE = "1",
  PENDING = "0",
  HIDDEN = "-1",
}

export enum Order {
  ASC = "ASC",
  DESC = "DESC",
}
